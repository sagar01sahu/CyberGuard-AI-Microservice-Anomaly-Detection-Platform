spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/cyberguard_db
    username: postgres
    password: securepassword123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080